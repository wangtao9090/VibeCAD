adapter: generic

capability_profile: artifact-approval / serial / repo-artifact / blocking-command

model_tier: standard（无可验证的模型选择）

approval_gate: 核验 repo artifact 的当前修订及既有用户授权；范围未变则不重复请求批准

exact_tools: repo read/write；blocking shell

state_source: repo artifact

next_action: 核验仓库状态、artifact 修订和授权边界后，串行执行下一已批准任务包；完成每个 commit 的 gate 执行与独立 review，再提交并推送

continue_or_stop: continue