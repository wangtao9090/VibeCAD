# VibeCAD Round 10 — MCPB 一键安装 + 官方目录上架 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development（逐任务 实现→审查→修复）。Steps 用 `- [ ]` 勾选。

**Goal:** 用户下载 `VibeCAD.mcpb` 双击即装（零终端/零配置文件/零 uv 预装），一次 tag 同步发布 PyPI + GitHub Release，并完成官方连接器目录提交材料。

**Architecture:** 仓库根 `manifest.json`（mcpb `uv` 类型：包内只带 pyproject.toml + src/，宿主自动管理 Python/依赖）+ `.mcpbignore` 控制内容物；release.yml 扩展 `mcpb pack` + GitHub Release；快测锁三处版本同步与 manifest/server 工具表一致性。**Task 0 spike 是硬门禁**：uv 类型在用户真机 Claude Desktop 跑通才继续，否则回退 Node 包装器方案（重新评审计划）。

**Tech Stack:** @anthropic-ai/mcpb CLI（npx，本机/CI 均有 node）、GitHub Actions（gh release）、FastMCP。

**Spec:** `docs/superpowers/specs/2026-06-10-round10-mcpb-onboarding-design.md`

---

## File Structure

```
manifest.json                  新建  mcpb manifest（uv 类型，22 工具表，零 user_config）
.mcpbignore                    新建  排除 tests/docs/.github/.vibecad*/dist 等
mcpb_entry.py                  新建  扩展入口（薄：launcher.main()）——Task 0 验证后可能改为直指 src/vibecad/__main__.py
PRIVACY.md                     新建  隐私政策（本地处理/运行时下载源/无遥测）
tests/test_mcpb_manifest.py    新建  版本三处同步 + manifest 工具表 == server 注册工具 + 内容物守卫
src/vibecad/server.py          改    FastMCP serverInfo.version 报包版本（36 行附近）
.github/workflows/release.yml  改    + setup-node + mcpb validate/pack + gh release create 附 .mcpb
.github/workflows/ci.yml       改    lint-unit 加 npx mcpb validate（manifest 回归防线）
docs/USER_GUIDE.md             改    主路径改"下载→双击→开聊"，uv/配置文件法降级附录
docs/ACCEPTANCE_TESTS.md       改    A1/A2 入口改双击安装
README.md                      改    快速开始改 Release 下载引流
pyproject.toml                 改    version 0.2.0
src/vibecad/__init__.py        改    __version__ 0.2.0
.vibecad/spike_mcpb/           新建  Task 0 spike 目录（gitignore 内）
```

---

## Task 0：Spike——uv 类型 .mcpb 真机验证（硬门禁，控制者执行）

**Files:** Create `.vibecad/spike_mcpb/manifest.json`、`.vibecad/spike_mcpb/pyproject.toml`、`.vibecad/spike_mcpb/src/spike_server.py`

- [ ] **Step 1**: 装 CLI 并初始化最小包（不污染全局：用 npx）：

```bash
mkdir -p .vibecad/spike_mcpb/src && cd .vibecad/spike_mcpb
npx -y @anthropic-ai/mcpb --version   # 确认 CLI 可用，记录版本
```

`.vibecad/spike_mcpb/src/spike_server.py`（最小 FastMCP，验证宿主能拉起 uv 环境）：

```python
"""mcpb uv 类型 spike：单工具 server，验证宿主自动管理 Python/依赖。"""
from mcp.server.fastmcp import FastMCP

mcp = FastMCP("vibecad-spike")


@mcp.tool()
def spike_ping() -> str:
    """连通性探针。"""
    import sys
    return f"spike ok, python={sys.version}, exe={sys.executable}"


if __name__ == "__main__":
    mcp.run()
```

`.vibecad/spike_mcpb/pyproject.toml`：

```toml
[project]
name = "vibecad-spike"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = ["mcp>=1.12"]
```

`.vibecad/spike_mcpb/manifest.json`：

```json
{
  "manifest_version": "0.4",
  "name": "vibecad-spike",
  "display_name": "VibeCAD Spike",
  "version": "0.0.1",
  "description": "uv-type mcpb spike probe",
  "author": { "name": "Wang Tao" },
  "server": {
    "type": "uv",
    "entry_point": "src/spike_server.py"
  },
  "compatibility": {
    "platforms": ["darwin", "win32"]
  }
}
```

- [ ] **Step 2**: `cd .vibecad/spike_mcpb && npx -y @anthropic-ai/mcpb validate manifest.json && npx -y @anthropic-ai/mcpb pack` → 产出 `.mcpb`。若 validate 报 manifest 字段错误（规范演进），按 CLI 报错修字段并把修正回填本计划。
- [ ] **Step 3**: **交用户双击安装**（约定：不替用户操作本机）——请用户：双击 .mcpb → 截图安装弹窗 → 安装后新对话问"你能看到 vibecad-spike 的工具吗"→ 调 spike_ping 把返回贴回来。验证三点：①uv 类型被当前版 Claude Desktop 识别；②宿主自动建好 Python 环境（spike_ping 返回的 python/exe 路径）；③Cowork 模式同样可见。
- [ ] **Step 4**: 第二段 spike——把 spike 的 pyproject dependencies 换成 `["vibecad==0.1.0"]`、entry 改 `from vibecad.launcher import main; main()`，重打包请用户再装一次：验证 **launcher re-exec（os.execv 进 conda python）在扩展宿主进程树下 stdio 不断**（ensure_runtime 已就绪的机器上 ping → ensure/status → smoke_cad）。⚠️ 用户机器 FreeCAD 运行时会装到默认 VIBECAD_HOME（~/Library/Application Support/VibeCAD）——属正常用户路径，无需清理。
- [ ] **Step 5**: 结论回填本计划此处；**若 ①或④ 失败 → 停，回退 Node 包装器方案重新评审计划**（不要硬干）。请用户在 Claude Desktop 里卸载 spike 扩展。
- [ ] **Step 6**: commit（仅计划回填；spike 目录在 .vibecad/ gitignore 内）

---

## Task 1：manifest.json + .mcpbignore + 入口 + 一致性快测

**Files:** Create `manifest.json`、`.mcpbignore`、`mcpb_entry.py`、`tests/test_mcpb_manifest.py`

- [ ] **Step 1: 失败测试**

```python
# tests/test_mcpb_manifest.py
"""mcpb manifest 守卫：版本三处同步、工具表与 server 注册一致、内容物排除。快测。"""
import json
import re
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent


def _manifest() -> dict:
    return json.loads((ROOT / "manifest.json").read_text(encoding="utf-8"))


def test_version_synced_three_ways():
    m = _manifest()
    pyproject = (ROOT / "pyproject.toml").read_text(encoding="utf-8")
    py_ver = re.search(r'^version = "([^"]+)"', pyproject, re.M).group(1)
    init = (ROOT / "src/vibecad/__init__.py").read_text(encoding="utf-8")
    init_ver = re.search(r'__version__ = "([^"]+)"', init).group(1)
    assert m["version"] == py_ver == init_ver


def test_manifest_tools_match_server_registry():
    """manifest.tools 必须与 server 注册的工具完全一致（防加工具忘更新 manifest）。"""
    import vibecad.server as server
    registered = {t.name for t in server.mcp._tool_manager.list_tools()}
    declared = {t["name"] for t in _manifest()["tools"]}
    assert declared == registered, (
        f"manifest 缺 {registered - declared}，多 {declared - registered}")


def test_manifest_uv_type_and_entry():
    m = _manifest()
    assert m["server"]["type"] == "uv"
    assert (ROOT / m["server"]["entry_point"]).exists()
    assert "user_config" not in m  # 设计：零配置表单


def test_mcpbignore_excludes_heavy_dirs():
    ignore = (ROOT / ".mcpbignore").read_text(encoding="utf-8")
    for pattern in ("tests/", "docs/", ".github/", ".vibecad", "dist/"):
        assert pattern in ignore, f".mcpbignore 缺 {pattern}"
```

（`server.mcp._tool_manager.list_tools()` 若 FastMCP 版本不暴露该私有属性，改用 `asyncio.run(server.mcp.list_tools())` 取公开 API——以实测可行者为准，意图：从 server 拿全量注册工具名。）

- [ ] **Step 2**: 跑测试确认失败（manifest.json 不存在）：`uv run pytest tests/test_mcpb_manifest.py -v` → FAIL。
- [ ] **Step 3: 实现**。`mcpb_entry.py`（Task 0 结论若支持直指 `src/vibecad/__main__.py` 则省略此文件并改 manifest）：

```python
"""mcpb 扩展入口：宿主 uv 环境中拉起 VibeCAD MCP server（stdio）。"""
from vibecad.launcher import main

if __name__ == "__main__":
    main()
```

`manifest.json`（22 个工具描述取自 server.py 各工具 docstring 首句，中文）：

```json
{
  "manifest_version": "0.4",
  "name": "vibecad",
  "display_name": "VibeCAD",
  "version": "0.2.0",
  "description": "AI 对话式 CAD：用自然语言画零件、打孔、装配、导出 3D 打印文件（FreeCAD 引擎）",
  "long_description": "给一年只设计几次东西的人：免费的 FreeCAD + 一个让你永远不用学 FreeCAD 的 AI。每步自动回工程图三视图，支持参数修改、孔阵列、多零件装配与干涉检查，导出 STEP/STL/3MF。首次使用自动下载建模引擎（约 2-3GB，仅一次）。",
  "author": { "name": "Wang Tao", "email": "wangtao9090@gmail.com" },
  "license": "MIT",
  "homepage": "https://github.com/wangtao9090/VibeCAD",
  "repository": { "type": "git", "url": "https://github.com/wangtao9090/VibeCAD" },
  "server": {
    "type": "uv",
    "entry_point": "mcpb_entry.py"
  },
  "compatibility": {
    "platforms": ["darwin", "win32"]
  },
  "tools": [
    { "name": "ping", "description": "连通性检查" },
    { "name": "get_runtime_status", "description": "查询 FreeCAD 运行时安装状态" },
    { "name": "ensure_runtime", "description": "确保 FreeCAD 运行时就绪（未装则后台安装）" },
    { "name": "smoke_cad", "description": "冒烟测试：建 Box 并导出 STEP" },
    { "name": "new_document", "description": "新建单零件工作文档" },
    { "name": "add_box", "description": "添加参数化长方体" },
    { "name": "add_cylinder", "description": "添加参数化圆柱" },
    { "name": "boolean_cut", "description": "布尔差集（从 base 减去 tool）" },
    { "name": "export_part", "description": "导出 STEP/STL/glTF 可制造文件" },
    { "name": "describe_part", "description": "文本诊断：体积/包围盒/质心" },
    { "name": "render_part", "description": "渲染 PNG 预览图/工程图三视图/标注图" },
    { "name": "add_hole", "description": "在指定面打圆孔（支持阵列）" },
    { "name": "fillet_edges", "description": "边圆角" },
    { "name": "chamfer_edges", "description": "边倒角" },
    { "name": "modify_part", "description": "修改已有对象参数（依赖链自动重算）" },
    { "name": "move_part", "description": "移动图元到绝对位置" },
    { "name": "rotate_part", "description": "旋转图元（绕包围盒中心）" },
    { "name": "extrude_profile", "description": "自由轮廓拉伸 pad/pocket" },
    { "name": "new_part", "description": "新建装配零件并设为活动零件" },
    { "name": "set_active_part", "description": "切换活动零件" },
    { "name": "place_part", "description": "零件级位姿（整零件移动/旋转）" },
    { "name": "align_parts", "description": "面贴面对齐（自动干涉检查）" }
  ]
}
```

`.mcpbignore`：

```
tests/
docs/
.github/
.vibecad/
.vibecad-test-runtime/
dist/
uv.lock
.gitignore
.python-version
CLAUDE.md
*.mcpb
```

- [ ] **Step 4**: `uv run pytest tests/test_mcpb_manifest.py -v` 全 PASS；`npx -y @anthropic-ai/mcpb validate manifest.json` 通过；`npx -y @anthropic-ai/mcpb pack . dist/VibeCAD.mcpb` 产包成功且 `unzip -l dist/VibeCAD.mcpb` 人眼确认无 tests/docs/.vibecad（内容物守卫）。
- [ ] **Step 5**: `uv run pytest -q && uv run ruff check .` 既有全绿。
- [ ] **Step 6: commit** `feat(mcpb): one-click desktop extension manifest + consistency tests`

---

## Task 2：serverInfo.version 报包版本 + 版本升 0.2.0

**Files:** Modify `src/vibecad/server.py:36`、`pyproject.toml:3`、`src/vibecad/__init__.py`；Modify `tests/test_server_handshake.py`（或既有 server 测试文件中握手相关处，以现状为准）

- [ ] **Step 1: 失败测试**（加进既有 server 测试文件）：

```python
def test_server_reports_package_version():
    """serverInfo.version 必须报 vibecad 包版本而非 mcp SDK 版本（R9 发现）。"""
    import vibecad
    import vibecad.server as server
    opts = server.mcp._mcp_server.create_initialization_options()
    assert opts.server_version == vibecad.__version__
```

（FastMCP 内部属性名 `_mcp_server` 若不符，用 `dir()` 探明低层 Server 实例取 initialization options——意图：锁 initialize 响应的 serverInfo.version。）

- [ ] **Step 2**: 实现：`FastMCP("vibecad")` 改为读包版本（FastMCP 是否接受 version 参数以实测为准；不接受则构造后设置低层 server 的 version 属性）：

```python
from vibecad import __version__

mcp = FastMCP("vibecad", version=__version__)
```

- [ ] **Step 3**: pyproject `version = "0.2.0"`、`__init__.py` `__version__ = "0.2.0"`（manifest 已是 0.2.0，三处同步测试守护）。
- [ ] **Step 4**: `uv run pytest -q && uv run ruff check .` 全绿。
- [ ] **Step 5: commit** `fix(server): report package version in serverInfo; bump 0.2.0`

---

## Task 3：release.yml 扩展 + ci.yml validate

**Files:** Modify `.github/workflows/release.yml`、`.github/workflows/ci.yml`

- [ ] **Step 1**: release.yml 全量替换为：

```yaml
name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  pypi:
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
      - run: uv build
      - uses: pypa/gh-action-pypi-publish@release/v1
  mcpb:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # 创建 GitHub Release
    steps:
      - uses: actions/checkout@v4
      - run: npx -y @anthropic-ai/mcpb validate manifest.json
      - run: npx -y @anthropic-ai/mcpb pack . VibeCAD.mcpb
      - name: create GitHub Release with .mcpb asset
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" VibeCAD.mcpb \
            --title "VibeCAD ${GITHUB_REF_NAME}" \
            --generate-notes
```

（pypi 与 mcpb 两 job 并行独立：PyPI 失败不阻塞 Release，反之亦然；ubuntu-latest 自带 node 20+。）

- [ ] **Step 2**: ci.yml 的 lint-unit steps 末尾加 manifest 回归防线（仅 ubuntu 跑一次即可，矩阵全跑浪费）：

```yaml
      - run: npx -y @anthropic-ai/mcpb validate manifest.json
        if: matrix.os == 'ubuntu-latest'
```

- [ ] **Step 3**: 推送后看 ci run 绿（lint-unit 含 validate 步）。
- [ ] **Step 4: commit** `ci(release): pack .mcpb and attach to GitHub Release on tag`

---

## Task 4：PRIVACY.md + 上架材料

**Files:** Create `PRIVACY.md`；Modify `manifest.json`（如目录提交要求补充字段，按官方清单）

- [ ] **Step 1**: `PRIVACY.md`（中英双语，目录审核要英文）：

```markdown
# Privacy Policy / 隐私政策

**VibeCAD processes everything locally. / VibeCAD 所有处理均在本地完成。**

- All geometry, models, and exported files are created and stored on your machine only.
  所有几何数据、模型与导出文件仅在你的电脑上创建与保存，不上传任何服务器。
- On first use, VibeCAD downloads the FreeCAD runtime (~2-3 GB) from official
  open-source mirrors (micromamba / conda-forge). No personal data is sent.
  首次使用时从官方开源镜像（micromamba / conda-forge）下载 FreeCAD 运行时（约 2-3GB），
  不携带任何个人数据。
- VibeCAD collects no telemetry, no analytics, no account information.
  VibeCAD 不收集遥测、统计或账号信息。
- Network access happens only for the one-time runtime download above.
  网络访问仅发生在上述一次性运行时下载。

Contact / 联系：wangtao9090@gmail.com · https://github.com/wangtao9090/VibeCAD
```

- [ ] **Step 2**: 对照官方目录提交清单（https://claude.com/docs/connectors 提交入口/表单，以提交页实际要求为准）整理：扩展名/描述/图标（无则用文字 logo 占位，记 backlog）/隐私政策链接（仓库 PRIVACY.md raw 链接）/工具标注（manifest tools 已含 description；如要求 readOnly/destructive 注解，给 22 工具补 `annotations` 字段：ensure_runtime/export_part/smoke_cad 标写盘，其余建模工具标"修改会话内模型"）。
- [ ] **Step 3**: **提交动作涉及开发者账号/表单，整理成"提交操作卡"交用户执行**（约定：账号类操作不代办）；提交后状态记入收尾汇报。审核结果不阻塞本轮验收。
- [ ] **Step 4: commit** `docs: privacy policy for connectors directory submission`

---

## Task 5：手册/验收/README 改版

**Files:** Modify `docs/USER_GUIDE.md`、`docs/ACCEPTANCE_TESTS.md`、`README.md`

- [ ] **Step 1**: USER_GUIDE 改版（结构调整，正文沿用既有口吻）：
  - 第二章「安装前提」删去"装 uv"主步骤 → 改为只需 Claude Desktop（含 Cowork）+ 磁盘 ≥5GB；
  - 第三章改为「安装 VibeCAD（双击即装）」：①打开 Release 页 `https://github.com/wangtao9090/VibeCAD/releases/latest` 下载 `VibeCAD.mcpb` ②双击 → Claude Desktop 弹窗 → 点安装 ③重启/新开对话验证（话术不变）。截图占位 3 处（下载页/安装弹窗/扩展列表）；
  - 原"装 uv + 配置文件/Claude Code 命令"整体移到附录 A「其他 MCP 客户端接入（Claude Code / Cursor 等）」；
  - 故障排查表补三行：扩展更新方式=下载新版重新双击；扩展日志位置（Task 0 实测路径回填）；"安装弹窗没出现"=Claude Desktop 版本过旧请更新。
- [ ] **Step 2**: ACCEPTANCE_TESTS：A1 改"双击安装 .mcpb 后 Settings→Extensions 可见 VibeCAD 且启用"；A2 不变（运行时下载体验一致）；B 部分 Windows 清单第一步改"下载 VibeCAD.mcpb 双击安装"。
- [ ] **Step 3**: README「快速开始（用户）」改为：下载 [`VibeCAD.mcpb`](https://github.com/wangtao9090/VibeCAD/releases/latest) 双击安装；uvx/Claude Code 方式保留为次选一行。
- [ ] **Step 4**: 控制者产品视角审稿。
- [ ] **Step 5: commit** `docs: switch primary install path to one-click .mcpb`

---

## Task 6：发布演练 + 真机验收（控制者）

- [ ] **Step 1**: 预发演练：`git tag v0.2.0-rc1 && git push origin v0.2.0-rc1` → 确认 release run 双 job 绿、GitHub Release（prerelease 自动 notes）带 `VibeCAD.mcpb`、PyPI 出 0.2.0rc1（注意 PEP 440：tag 名与 pyproject version 不一致仅影响 PyPI 版本号显示——pyproject 是 0.2.0，rc tag 下 PyPI 会收到 0.2.0 正式版！**为避免误发正式版：演练 tag 前临时把 release.yml 的 pypi job 限定 `if: ${{ !contains(github.ref_name, '-rc') }}`**，演练只验 mcpb job；该 if 长期保留，rc tag 永不发 PyPI）。
- [ ] **Step 2**: 从 Release 页下载 .mcpb，**交用户 macOS 双击安装** → 跑 ACCEPTANCE_TESTS A1-A4 → 反馈安装弹窗截图校正手册截图位文案。
- [ ] **Step 3**: 用户 Windows 机器同样下载双击 → A1/A2/A4 三条。
- [ ] **Step 4**: 验收过 → 删 rc：`git push origin :v0.2.0-rc1`（Release 页同步删 rc release）→ 正式 `git tag v0.2.0 && git push origin v0.2.0` → PyPI 0.2.0 + 正式 Release 双绿 → 干净 `uvx vibecad@0.2.0` 握手复验（附录路径不回归）。
- [ ] **Step 5**: 收尾自检：302+新增 fast 全绿；65 slow 全绿（⚠️ .vibecad-test-runtime 已删，首跑自动重装 2-3GB，预留时间，国内网络配 UV/conda 镜像见 memory uv-cn-mirror）。

## Task 7：收尾（控制者）

- [ ] 上架「提交操作卡」交用户（提交页地址/逐字段填写内容/PRIVACY 链接）；提交状态记录。
- [ ] README 状态行加 R10；飞书同步 spec/计划/改版手册；memory 更新 R10 终态；最终汇报。

---

## 风险

1. **uv 类型宿主行为未知数最大**（Task 0 硬门禁先行，回退 Node 包装器）；entry_point 能否 import 包内模块（uv run 项目环境）spike 一并验证。
2. mcpb CLI 规范演进快——validate 进 CI 锁住；CLI 版本固定用 `npx -y @anthropic-ai/mcpb@latest` 还是锁版本，Task 0 按实测稳定性定。
3. 用户双击环节依赖用户配合节奏——A1-A4 验收异步进行，不阻塞其他 Task。
4. rc 演练误发 PyPI——Task 6 Step 1 的 `if` 守卫先行。

## Verification

1. 快测：`uv run pytest -q` 全绿（含 manifest 一致性 + serverInfo.version）。
2. 慢测：`VIBECAD_RUN_INTEGRATION=1 uv run pytest -m slow -q` 全绿（行为零变化回归）。
3. 真机：用户 macOS + Windows 双击安装跑 A1-A4。
4. 发布链路：一次 tag → PyPI + GitHub Release（.mcpb 附件）双绿。
5. 上架：材料齐备 + 用户完成提交（审核结果不作验收）。
